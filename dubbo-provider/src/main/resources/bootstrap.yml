dubbo:
  scan:
    base-packages: com.chenming.cloud.provider.service
  protocol:
    name: dubbo
    port: -1

spring:
  application:
    name: dubbo-provider
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848