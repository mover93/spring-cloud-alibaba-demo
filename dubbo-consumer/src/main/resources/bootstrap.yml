server:
  port: 8090

dubbo:
  cloud:
    subscribed-services: dubbo-provider
  protocols:
    dubbo:
      port: -1

spring:
  application:
    name: dubbo-consumer
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      username: nacos
      password: nacos
      discovery:
        server-addr: 127.0.0.1:8848